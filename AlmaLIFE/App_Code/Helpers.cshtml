
@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<AlmaLIFE.Models.Tag> tags)
{
    foreach (var tag in tags)
    {
    <div class="tags-div">
        @AlmaLIFE.ActionLinkExtensions.TagLink(htmlHelper, tag)
    </div>
    }
}